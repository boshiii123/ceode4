import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Contact Us - WebP Compressor Support | CompressLab',
  description: 'Contact CompressLab for WebP compressor support, feedback, and inquiries. Get help with our free online WebP image compression tool and format conversion.',
  keywords: ['webp compressor support', 'contact compresslab', 'image compression help', 'webp compression support', 'webp converter support', 'online compressor help'],
}

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
} 