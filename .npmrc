# NPM configuration for cross-platform compatibility
target_platform=
target_arch=
target_libc=

# Ensure optional dependencies work across platforms
optional=true
legacy-peer-deps=false

# Cache settings
cache=/tmp/.npm
audit-level=moderate 